<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {{link-to 'Conversation' 'index' class="navbar-brand"}}
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>{{link-to 'My Space' 'users.show' session.currentUser.id}}</li>
                <li>{{link-to 'About' 'about'}}</li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {{#if session.isAuthenticated}}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                           aria-haspopup="true" aria-expanded="false">{{session.currentUser.name}}
                            <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a {{action 'invalidateSession'}} style="cursor: pointer;">Logout</a></li>
                        </ul>
                    </li>
                {{else}}
                    <li>{{link-to 'Log in' 'login'}}</li>
                    <li>{{ link-to "Sign up" 'users.new'}}</li>
                {{/if}}
            </ul>
        </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
</nav>